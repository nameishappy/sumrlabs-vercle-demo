"use client";
import React from "react";
const Features = () => {
  // const { scrollYProgress } = useScroll();
  return (
    <div >
      {/* {[0, 1, 2].map((index) => (
        <FeatureComponent key={index} index={index} />
      ))} */}
    </div>
  );
};
export default Features;